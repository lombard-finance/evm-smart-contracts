name: Run ratio check

on:
  workflow_dispatch:

jobs:
    setup-env:
        runs-on: [self-hosted]

        steps:
            - uses: actions/checkout@v3

            - name: Use Node.js
              uses: actions/setup-node@v3
              with:
                  node-version: '22.16.0'

            - name: Cache node modules
              uses: actions/cache@v3
              env:
                  cache-name: cache-node-modules
              with:
                  path: ~/.npm
                  key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
                  restore-keys: |
                      ${{ runner.os }}-build-${{ env.cache-name }}-
                      ${{ runner.os }}-build-
                      ${{ runner.os }}-

            - name: Install dependencies
              run: yarn install --frozen-lockfile

            - name: Compile contracts
              run: yarn hardhat compile

            - name: Check
              run: for net in mainnet base bsc berachain sonic ink; do yarn hardhat ratio check --network $net; done

            - name: Check community chains
              run: for net in swell cornMaizenet; do yarn hardhat ratio check mainnet-external.json --network $net; done